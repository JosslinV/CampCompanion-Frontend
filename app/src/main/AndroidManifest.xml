<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.azilab.campCompanion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Default">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC-nJxemv_30ka4lN0vQYcoqolqlwFKIKM"/>
        <activity android:name=".MainActivity" android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AddSpotActivity" android:label="Ajouter un spot" android:theme="@style/Default">
        </activity>

        <activity android:name=".SpotInfoActivity" android:theme="@style/Default">
        </activity>

        <activity android:name=".SearchPageActivity" android:label="Rechercher un spot" android:theme="@style/Default">
        </activity>

        <activity android:name=".ResultPageActivity" android:label="Résultat" android:theme="@style/Default">
        </activity>

        <activity android:name=".ShowSpotActivity" android:theme="@style/NoActionBar">
        </activity>

    </application>

    <!-- Location permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

</manifest>